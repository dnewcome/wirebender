py/
__pycache__/
*.pyc
sim/preview/
MUJOCO_LOG.TXT

# ── generated artifacts (reproduce from source; never edit by hand) ──
# CAD outputs — regenerate: py/bin/python cad/<part>.py
build/
# MuJoCo meshes + model — regenerate: cd sim && ../py/bin/python make_mjcf_v2.py
sim/meshes/
sim/wirebender*.xml

# Never commit CAD solids/meshes anywhere in the tree — they're all either generated
# (build/, sim/meshes/) or purchased reference parts; none belong in git. force-add
# (git add -f) if you ever truly need one.
*.stl
*.step
*.stp
