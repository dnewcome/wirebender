py/
__pycache__/
*.pyc
sim/preview/
MUJOCO_LOG.TXT

# ── generated artifacts (reproduce from source; never edit by hand) ──
# CAD outputs — regenerate: py/bin/python cad/<part>.py
build/
# MuJoCo meshes + model — regenerate: cd sim && ../py/bin/python make_mjcf_v2.py
sim/meshes/
sim/wirebender*.xml

# vendor cycloid geometry (Sweep Dynamics, paid) — not redistributed. No longer read by
# the build: cyclo_base.py is a parametric, vendor-independent rebuild. Kept only as a
# local reference; safe to delete.
nema-17-cycloid-base.stl
